node_modules/
dist/
.next/
next-env.d.ts
out/
packages/frontend/itch-build.zip
cdk.out/
coverage/
*.tsbuildinfo
.env
.env.local
.env.*.local
.DS_Store

# スプライト検証用のWesnothアセット(GPL)。fetch-demo-sprites.mjs でローカル取得する
# (本番はNEXT_PUBLIC_ASSET_BASE経由でwesnoth-contents-deliveryから配信。ローカルdev用の
# フォールバック取得先としてのみ使う。2026-07-10: 組み込みバンドル方式は撤去した)
packages/frontend/public/sprites/
# スプライトパック(A-4)。build-sprite-packs.mts が public/sprites/ から生成(GPL画像を含む)
packages/frontend/public/packs/

# Playwright
packages/frontend/test-results/
packages/frontend/playwright-report/

# Claude Code: worktreeとローカル設定はコミットしない(skillsはコミットする)
.claude/worktrees/
.claude/settings.local.json
site/

# LibreOffice/OpenOfficeのロックファイル(docs/terrain_audit/*.csv編集中に生成される)
.~lock.*#
